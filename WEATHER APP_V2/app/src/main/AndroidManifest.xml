<?xml version="1.0" encoding="utf-8"?>
<!-- D:/Claude/ANDROID-APP-DEV/WEATHER APP_V2/app/src/main/AndroidManifest.xml -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 網路存取：呼叫 Open-Meteo API 所需 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- 定位權限：取得裝置目前位置的天氣資訊 -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <!-- Android 13 (API 33) 起，發送通知（每日天氣提醒）需要執行期權限 -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".WeatherApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.WeatherForecastV2">

        <!--
          AdMob Application ID：此處先填入 Google 官方公開的「測試用」App ID，
          確保專案可直接編譯並顯示測試廣告。正式上架前請務必替換成你自己
          AdMob 後台（對應廣告單元 ca-app-pub-1512317781873771/6879519480）的正式 App ID，
          否則廣告將無法正確歸戶／可能無法送達。
        -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-3940256099942544~3347511713" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.WeatherForecastV2">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
